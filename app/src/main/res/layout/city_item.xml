<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="city"
            type="com.techninja.sensei.weatherapp.Models.WeatherResponse"/>
        <import type="com.techninja.sensei.weatherapp.Utilities.Formatter"/>
        <import type="com.techninja.sensei.weatherapp.Models.MainViewModel"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/city_name"
                android:layout_width="280dp"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:text="@{city.name}"
                android:textSize="20sp" />

            <TextView
                android:id="@+id/temperature"
                android:layout_width="62dp"
                android:layout_height="wrap_content"
                android:text="@{Formatter.FormatTemp(city.main.temp)}"
                android:textSize="18sp" />

            <ImageView
                android:id="@+id/weather_icon"
                android:layout_width="50dp"
                android:layout_height="fill_parent"
                android:scaleX="0.6"
                android:scaleY="0.6"
                android:tint="@color/colorPrimaryDark"
                android:contentDescription="Weather Icon"
                bind:imageResource="@{MainViewModel.GetIcon(city.weather[0].icon)}"
            />
        </LinearLayout>
    </LinearLayout>
</layout>